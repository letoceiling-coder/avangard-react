# Руководство по автоматическим коммитам

## 📋 Описание

Настроена автоматическая система коммитов и отправки изменений в Git. При каждом изменении можно автоматически создавать коммит с осмысленным описанием и отправлять его в репозиторий.

## 🚀 Использование

### Вариант 1: Автоматический коммит через npm скрипт

```bash
npm run commit
```

Этот скрипт:
- ✅ Анализирует все изменения в проекте
- ✅ Автоматически добавляет все файлы (`git add -A`)
- ✅ Создает коммит с осмысленным сообщением на основе изменений
- ✅ Автоматически отправляет в репозиторий (через post-commit hook)

### Вариант 2: Коммит и push одной командой

```bash
npm run commit:push
```

Выполняет коммит и принудительно отправляет изменения.

### Вариант 3: Автоматический push после любого коммита

После любого коммита (вручную или через скрипт) автоматически срабатывает `post-commit` hook, который отправляет изменения в репозиторий.

```bash
git commit -m "Ваше сообщение"
# Автоматически выполнится: git push origin <branch>
```

## 🔍 Как работает анализ изменений

Скрипт `auto-commit.js` анализирует:

1. **Типы файлов:**
   - Компоненты (`/components/`)
   - Страницы (`/pages/`)
   - Стили (`.css`, `.scss`, `.less`)
   - Конфигурация (`config`, `vite.config`, etc.)
   - Скрипты (`/scripts/`)
   - Документация (`.md`, `.txt`)

2. **Содержимое изменений:**
   - Поиск ключевых слов (`mobile-first`, `flex-col`, `HeroSection`, etc.)
   - Определение типа изменений (стили, layout, компоненты)

3. **Генерация сообщения:**
   - Объединяет информацию о типах файлов
   - Добавляет детали о конкретных изменениях
   - Указывает количество измененных файлов

## 📝 Примеры сообщений коммитов

```
Обновлен компонент HeroSection | Применен mobile-first подход, Использован flex-col layout (1 файлов)

Обновлены компоненты: HeroSection, HeroSearch | Вынесены компоненты Hero (2 файлов)

Обновлена страница Index | Применен mobile-first подход, Оптимизированы отступы, Ограничена ширина контейнеров (1 файлов)

Обновлены компоненты: PropertyCard, SearchWidget; Обновлена страница Index | Оптимизированы отступы (3 файлов)
```

## ⚙️ Настройка

### Отключение автоматического push

Если вы хотите отключить автоматический push после коммита, удалите или переименуйте файл:

```bash
mv .git/hooks/post-commit .git/hooks/post-commit.disabled
```

### Изменение правил анализа

Отредактируйте файл `scripts/auto-commit.js` для добавления своих правил анализа изменений.

### Настройка веток для автоматического push

По умолчанию автоматический push работает для:
- `main`
- `master`
- `develop`
- Веток с префиксом `feature/`, `fix/`, `hotfix/`

Для изменения этого поведения отредактируйте `.git/hooks/post-commit`.

## 🔒 Безопасность

⚠️ **Важно:** Автоматический push может быть опасен, если:
- Вы работаете в команде и хотите проверить изменения перед push
- У вас есть локальные изменения, которые не должны попасть в репозиторий

**Рекомендации:**
1. Используйте `npm run commit` только когда уверены в изменениях
2. Для важных изменений делайте коммиты вручную
3. Регулярно проверяйте статус: `git status`

## 📚 Дополнительные команды

```bash
# Проверить статус изменений
git status

# Посмотреть последние коммиты
git log --oneline -5

# Отменить последний коммит (но оставить изменения)
git reset --soft HEAD~1

# Отменить последний коммит и изменения
git reset --hard HEAD~1
```

## 🐛 Решение проблем

### Ошибка: "Нет изменений для коммита"
- Убедитесь, что есть незакоммиченные изменения: `git status`

### Ошибка: "Permission denied" при push
- Проверьте настройки Git: `git config --get user.name`
- Убедитесь, что вы приняли приглашение Collaborator на GitHub

### Hook не срабатывает
- Проверьте права на выполнение: `chmod +x .git/hooks/post-commit`
- Убедитесь, что файл существует: `ls -la .git/hooks/post-commit`

## 💡 Советы

1. **Используйте автоматический коммит для мелких изменений:**
   - Исправления опечаток
   - Небольшие стилистические изменения
   - Обновления документации

2. **Делайте коммиты вручную для важных изменений:**
   - Новые функции
   - Изменения архитектуры
   - Исправления критических багов

3. **Регулярно проверяйте историю:**
   ```bash
   git log --oneline --graph -10
   ```

---

**Создано:** Автоматическая система коммитов  
**Версия:** 1.0.0

