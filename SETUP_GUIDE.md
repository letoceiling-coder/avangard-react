# Пошаговая инструкция по настройке проекта

## ✅ Шаг 1: Настройка Git (ВЫПОЛНИТЕ ВРУЧНУЮ)

Выполните следующие команды в терминале, заменив данные на свои:

```bash
cd /Users/neeklo/Documents/Project/САЙТЫ/LiveGrid/LIVEGRID/avangard-react

# Настройте ваше имя для Git
git config --global user.name "Ваше Имя"

# Настройте ваш email для Git
git config --global user.email "ваш-email@example.com"

# Проверьте настройки
git config --get user.name
git config --get user.email
```

## ✅ Шаг 2: Проверка подключения к репозиторию

Подключение уже настроено правильно:
- ✅ origin: https://github.com/letoceiling-coder/avangard-react.git
- ✅ upstream: https://github.com/letoceiling-coder/avangard-react.git

## ✅ Шаг 3: Установка зависимостей

Зависимости уже установлены (node_modules существует).

Если нужно переустановить:
```bash
npm install
```

## ⚠️ Шаг 4: Настройка переменных окружения

Проект использует Supabase. Создайте файл `.env` в корне проекта:

```bash
cd /Users/neeklo/Documents/Project/САЙТЫ/LiveGrid/LIVEGRID/avangard-react
touch .env
```

Добавьте в файл `.env` следующие переменные (получите их из Supabase проекта):

```
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key
```

**Где взять эти данные:**
1. Зайдите в ваш Supabase проект: https://supabase.com/dashboard
2. Перейдите в Settings → API
3. Скопируйте:
   - **Project URL** → `VITE_SUPABASE_URL`
   - **anon public** key → `VITE_SUPABASE_PUBLISHABLE_KEY`

**Примечание:** Если Supabase еще не настроен, проект будет работать, но функции авторизации не будут доступны.

## ✅ Шаг 5: Запуск проекта

```bash
# Запустите dev-сервер
npm run dev

# Проект откроется на http://localhost:8080
# (порт настроен в vite.config.ts)
```

## ✅ Шаг 6: Проверка работоспособности

После запуска `npm run dev`:
1. Откройте браузер на http://localhost:8080
2. Убедитесь, что сайт загружается
3. Проверьте консоль браузера на наличие ошибок

## 📝 Шаг 7: Работа с изменениями

У вас есть незакоммиченные изменения:
- `src/pages/Index.tsx` (изменение заголовка)
- `package-lock.json`

### Чтобы закоммитить изменения:

```bash
# Добавить изменения
git add src/pages/Index.tsx

# Создать коммит
git commit -m "Изменен заголовок на главной странице"

# Отправить изменения
git push origin main
```

**Внимание:** После push в `main` автоматически запустится deploy через GitHub Actions.

## 🔍 Полезные команды

```bash
# Проверить статус Git
git status

# Посмотреть историю коммитов
git log --oneline

# Получить последние изменения
git pull origin main

# Создать новую ветку для работы
git checkout -b feature/название-функции

# Проверить установленные зависимости
npm list --depth=0
```

## ❓ Решение проблем

### Проблема: "Permission denied" при push
- Убедитесь, что вы приняли приглашение Collaborator на GitHub
- Проверьте настройки Git (user.name и user.email)

### Проблема: Проект не запускается
- Проверьте версию Node.js: `node --version` (должна быть 18+)
- Переустановите зависимости: `rm -rf node_modules && npm install`

### Проблема: Ошибки с Supabase
- Проверьте файл `.env` и правильность переменных
- Убедитесь, что Supabase проект активен

