# 🗺️ Где и что добавлять в GitHub - Визуальная инструкция

## 📍 Шаг 1: Открыть страницу Secrets

### Путь:
```
1. Откройте: https://github.com/letoceiling-coder/avangard-react
2. Нажмите на вкладку "Settings" (вверху справа, рядом с "Code", "Issues", "Pull requests")
3. В левом меню найдите раздел "Secrets and variables"
4. Нажмите на "Actions"
```

**Или прямая ссылка:**
```
https://github.com/letoceiling-coder/avangard-react/settings/secrets/actions
```

---

## ➕ Шаг 2: Добавить секреты

### На странице Secrets вы увидите:
- Кнопку: **"New repository secret"** (справа вверху)
- Список существующих секретов (если есть)

### Нажмите "New repository secret"

---

## 🔑 Секрет 1: SSH_PRIVATE_KEY

### Что добавить:
1. **Name:** `SSH_PRIVATE_KEY` (точно так, без пробелов)
2. **Secret:** 
   - Откройте файл: `C:\Users\ВашеИмя\.ssh\id_rsa`
   - Скопируйте ВСЁ содержимое (включая строки с `-----BEGIN` и `-----END`)
   - Вставьте в поле Secret

### Как выглядит:
```
Name: SSH_PRIVATE_KEY
Secret: -----BEGIN OPENSSH PRIVATE KEY-----
        b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAABlwAAAAdzc2gtcn
        ... (много строк) ...
        -----END OPENSSH PRIVATE KEY-----
```

### Нажмите: **Add secret**

---

## 🌐 Секрет 2: SERVER_HOST

### Что добавить:
1. **Name:** `SERVER_HOST`
2. **Secret:** `trendagent.siteaccess.ru` (ваш домен)

### Как выглядит:
```
Name: SERVER_HOST
Secret: trendagent.siteaccess.ru
```

### Нажмите: **Add secret**

---

## 👤 Секрет 3: SERVER_USER

### Что добавить:
1. **Name:** `SERVER_USER`
2. **Secret:** ваш пользователь на сервере (например: `user` или `admin`)

**Как узнать пользователя:**
- Это имя, которое вы используете для SSH: `ssh user@trendagent.siteaccess.ru`
- Обычно это имя вашего аккаунта на сервере

### Как выглядит:
```
Name: SERVER_USER
Secret: user
```

### Нажмите: **Add secret**

---

## 🔌 Секрет 4: SERVER_PORT

### Что добавить:
1. **Name:** `SERVER_PORT`
2. **Secret:** `22` (стандартный порт SSH)

**Если используете другой порт:**
- Узнайте у хостинг-провайдера
- Или проверьте в SSH клиенте, какой порт используется

### Как выглядит:
```
Name: SERVER_PORT
Secret: 22
```

### Нажмите: **Add secret**

---

## ✅ Проверка

### После добавления всех секретов вы должны увидеть список:

```
Repository secrets (4)
├── SSH_PRIVATE_KEY        [Update] [Delete]
├── SERVER_HOST            [Update] [Delete]
├── SERVER_USER            [Update] [Delete]
└── SERVER_PORT            [Update] [Delete]
```

---

## 🔍 Где проверить workflow

### Путь к Actions:
```
1. Откройте: https://github.com/letoceiling-coder/avangard-react
2. Нажмите на вкладку "Actions" (вверху, рядом с "Code", "Issues")
3. В левом меню выберите "Deploy to Production"
```

**Или прямая ссылка:**
```
https://github.com/letoceiling-coder/avangard-react/actions
```

---

## 📝 Где находится workflow файл

### Путь в репозитории:
```
.github/workflows/deploy.yml
```

### Как проверить:
1. Откройте репозиторий: https://github.com/letoceiling-coder/avangard-react
2. Нажмите на папку `.github`
3. Нажмите на папку `workflows`
4. Откройте файл `deploy.yml`

**Если файла нет:**
- Создайте папку `.github/workflows/`
- Создайте файл `deploy.yml`
- Скопируйте содержимое из файла, который я создал

---

## 🎯 Итоговая структура

### В GitHub должно быть:

**Secrets:**
```
Settings → Secrets and variables → Actions
├── SSH_PRIVATE_KEY
├── SERVER_HOST
├── SERVER_USER
└── SERVER_PORT
```

**Workflow:**
```
.github/workflows/deploy.yml
```

**Actions:**
```
Actions → Deploy to Production (после первого push)
```

---

## 🚀 После настройки

### Тест:
1. Сделайте любое изменение в коде
2. Выполните:
```bash
git add .
git commit -m "Test deploy"
git push origin main
```

3. Перейдите в **Actions**
4. Должен запуститься workflow
5. Через 2-3 минуты должен появиться зеленый значок ✅

---

## 🆘 Проблемы?

### "Secret not found"
- Проверьте, что имя секрета написано точно: `SSH_PRIVATE_KEY` (не `ssh_private_key`)
- Проверьте, что вы добавили секрет в правильный репозиторий

### "Permission denied"
- Проверьте, что приватный ключ скопирован полностью (с BEGIN и END)
- Проверьте, что публичный ключ на сервере

### Workflow не запускается
- Проверьте, что файл `.github/workflows/deploy.yml` существует
- Проверьте синтаксис YAML (должен быть валидным)
- Проверьте, что ветка называется `main`

---

**Готово! Теперь у вас есть все секреты в правильных местах! 🎉**

