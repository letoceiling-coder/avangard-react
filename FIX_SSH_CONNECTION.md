# 🔧 Исправление ошибки SSH подключения

## ❌ Проблема: "Host key verification failed"

Эта ошибка возникает, когда:
- Хост уже известен под другим именем (post-ads.ru)
- SSH видит конфликт в файле known_hosts

---

## ✅ Решение

### Вариант 1: Добавить хост в known_hosts (Рекомендуется)

**1. Ответьте "yes" на вопрос:**
```
Are you sure you want to continue connecting (yes/no/[fingerprint])?
```
Введите: `yes` и нажмите Enter

**2. Если это не помогло, используйте команду:**
```powershell
ssh-keyscan -H trendagent.siteaccess.ru >> C:\Users\dsc-2\.ssh\known_hosts
```

---

### Вариант 2: Удалить старую запись и добавить новую

**1. Откройте файл known_hosts:**
```powershell
notepad C:\Users\dsc-2\.ssh\known_hosts
```

**2. Найдите строку с `post-ads.ru` (строка 4)**
- Удалите эту строку или закомментируйте (добавьте `#` в начале)

**3. Сохраните файл**

**4. Попробуйте подключиться снова:**
```powershell
ssh user@trendagent.siteaccess.ru
```

**5. При запросе введите `yes`**

---

### Вариант 3: Использовать опцию для обхода проверки (временно)

**Для одноразового подключения:**
```powershell
ssh -o StrictHostKeyChecking=no user@trendagent.siteaccess.ru
```

**⚠️ Внимание:** Это менее безопасно, используйте только для первого подключения.

---

## 🔍 Проверка

**После исправления проверьте:**
```powershell
ssh user@trendagent.siteaccess.ru
```

**Должно подключиться без ошибок.**

---

## 📝 Что делать дальше

После успешного подключения:

1. **Скопируйте публичный ключ на сервер:**
```powershell
# Откройте публичный ключ
notepad C:\Users\dsc-2\.ssh\id_rsa.pub

# Скопируйте содержимое
# На сервере выполните:
ssh user@trendagent.siteaccess.ru
mkdir -p ~/.ssh
nano ~/.ssh/authorized_keys
# Вставьте скопированный ключ
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

2. **Проверьте подключение без пароля:**
```powershell
ssh user@trendagent.siteaccess.ru
# Должно подключиться БЕЗ запроса пароля
```

---

## 🆘 Если не помогло

**Проверьте файл known_hosts:**
```powershell
type C:\Users\dsc-2\.ssh\known_hosts
```

**Если нужно полностью очистить:**
```powershell
# Создайте резервную копию
copy C:\Users\dsc-2\.ssh\known_hosts C:\Users\dsc-2\.ssh\known_hosts.backup

# Очистите файл
echo. > C:\Users\dsc-2\.ssh\known_hosts

# Попробуйте подключиться снова
ssh user@trendagent.siteaccess.ru
# Введите "yes"
```

---

## ✅ Быстрое решение (скопируйте и выполните)

```powershell
# 1. Добавьте хост в known_hosts
ssh-keyscan -H trendagent.siteaccess.ru >> C:\Users\dsc-2\.ssh\known_hosts

# 2. Попробуйте подключиться
ssh user@trendagent.siteaccess.ru
```

Если появится вопрос - введите `yes`.

