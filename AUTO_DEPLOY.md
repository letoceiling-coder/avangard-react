# Автоматизация деплоя - Варианты реализации

## 🎯 Обзор вариантов

Выберите подходящий вариант в зависимости от ваших потребностей:

| Вариант | Сложность | Автоматизация | Рекомендация |
|---------|-----------|---------------|--------------|
| **1. GitHub Actions** | Средняя | Полная | ✅ Рекомендуется |
| **2. Git Post-Receive Hook** | Низкая | Полная | ✅ Простой вариант |
| **3. Webhook (PHP)** | Средняя | Полная | ✅ Для Apache |
| **4. Bash скрипт** | Низкая | Полуавтоматическая | Для ручного деплоя |
| **5. PM2 Deploy** | Средняя | Полная | Для Node.js окружения |

---

## 📋 Вариант 1: GitHub Actions (CI/CD) ⭐ РЕКОМЕНДУЕТСЯ

### Преимущества:
- ✅ Полностью автоматический деплой при push в main
- ✅ Работает из любой точки мира
- ✅ Не требует настройки на сервере
- ✅ Логи и история деплоев в GitHub
- ✅ Можно откатить изменения

### Настройка:

1. **Создайте секреты в GitHub:**
   - Перейдите: `Settings → Secrets and variables → Actions`
   - Добавьте секреты:
     - `SERVER_HOST` - IP или домен сервера
     - `SERVER_USER` - пользователь SSH
     - `SSH_PRIVATE_KEY` - приватный SSH ключ
     - `SERVER_PORT` - порт SSH (обычно 22)

2. **Создайте SSH ключ (если нет):**
```bash
ssh-keygen -t rsa -b 4096 -C "github-actions"
# Скопируйте публичный ключ на сервер
ssh-copy-id -i ~/.ssh/id_rsa.pub user@trendagent.siteaccess.ru
# Приватный ключ добавьте в GitHub Secrets как SSH_PRIVATE_KEY
```

3. **Файл уже создан:** `.github/workflows/deploy.yml`

4. **Использование:**
   - Просто делайте `git push origin main` - деплой запустится автоматически
   - Или запустите вручную: `Actions → Deploy to Production → Run workflow`

### Дополнительные улучшения:

Добавьте в `package.json`:
```json
{
  "scripts": {
    "deploy": "npm run build && node scripts/copy-files.js"
  }
}
```

---

## 📋 Вариант 2: Git Post-Receive Hook (На сервере)

### Преимущества:
- ✅ Простая настройка
- ✅ Работает сразу после push
- ✅ Не требует внешних сервисов
- ✅ Минимальные зависимости

### Настройка:

1. **На сервере создайте bare репозиторий:**
```bash
ssh user@trendagent.siteaccess.ru
cd ~
mkdir -p repos/avangard-react.git
cd repos/avangard-react.git
git init --bare
```

2. **Создайте post-receive hook:**
```bash
nano ~/repos/avangard-react.git/hooks/post-receive
# Скопируйте содержимое из scripts/post-receive-hook.sh
chmod +x ~/repos/avangard-react.git/hooks/post-receive
```

3. **Настройте локальный репозиторий:**
```bash
# Добавьте remote для деплоя
git remote add deploy user@trendagent.siteaccess.ru:~/repos/avangard-react.git
```

4. **Использование:**
```bash
git push deploy main
# Автоматически запустится деплой на сервере
```

---

## 📋 Вариант 3: Webhook (PHP) - Для Apache

### Преимущества:
- ✅ Работает с Apache без дополнительных сервисов
- ✅ Безопасность через секретный ключ
- ✅ Логирование всех деплоев
- ✅ Можно настроить уведомления

### Настройка:

1. **Разместите скрипт на сервере:**
```bash
# Скопируйте scripts/webhook-deploy.php на сервер
scp scripts/webhook-deploy.php user@trendagent.siteaccess.ru:~/trendagent.siteaccess.ru/public_html/webhook-deploy.php
```

2. **Настройте секретный ключ:**
```bash
ssh user@trendagent.siteaccess.ru
nano ~/trendagent.siteaccess.ru/public_html/webhook-deploy.php
# Измените $SECRET на свой секретный ключ
```

3. **Настройте webhook в GitHub:**
   - Перейдите: `Settings → Webhooks → Add webhook`
   - Payload URL: `https://trendagent.siteaccess.ru/webhook-deploy.php`
   - Content type: `application/json`
   - Secret: ваш секретный ключ
   - Events: `Just the push event`
   - Active: ✅

4. **Проверьте права доступа:**
```bash
chmod 755 ~/trendagent.siteaccess.ru/public_html/webhook-deploy.php
chmod 666 ~/deploy.log  # для логирования
```

5. **Использование:**
   - Просто делайте `git push origin main`
   - GitHub отправит webhook на ваш сервер
   - Деплой запустится автоматически

---

## 📋 Вариант 4: Bash скрипт (Полуавтоматический)

### Преимущества:
- ✅ Полный контроль над процессом
- ✅ Можно запускать вручную
- ✅ Легко модифицировать

### Использование:

1. **Сделайте скрипт исполняемым:**
```bash
chmod +x scripts/deploy.sh
```

2. **Настройте переменные окружения (опционально):**
```bash
export SERVER_USER="user"
export SERVER_HOST="trendagent.siteaccess.ru"
```

3. **Запустите деплой:**
```bash
./scripts/deploy.sh production
```

4. **Добавьте в package.json для удобства:**
```json
{
  "scripts": {
    "deploy": "./scripts/deploy.sh production"
  }
}
```

Теперь можно использовать: `npm run deploy`

---

## 📋 Вариант 5: PM2 Deploy

### Преимущества:
- ✅ Интеграция с PM2
- ✅ Автоматический деплой через PM2
- ✅ Управление через PM2 команды

### Настройка:

1. **Установите PM2 (если еще не установлен):**
```bash
npm install -g pm2
```

2. **Настройте SSH ключи:**
```bash
ssh-keygen -t rsa
ssh-copy-id user@trendagent.siteaccess.ru
```

3. **Используйте PM2 deploy:**
```bash
pm2 deploy scripts/pm2-deploy.js production setup
pm2 deploy scripts/pm2-deploy.js production
```

4. **Для автоматического деплоя при push:**
   - Используйте в комбинации с GitHub Actions или Webhook
   - Или настройте cron для периодической проверки

---

## 🔒 Безопасность

### Рекомендации для всех вариантов:

1. **SSH ключи:**
   - Используйте ключи без пароля для автоматизации
   - Ограничьте права доступа: `chmod 600 ~/.ssh/id_rsa`

2. **Секретные ключи:**
   - Никогда не коммитьте секреты в git
   - Используйте переменные окружения
   - Для GitHub Actions используйте Secrets

3. **Webhook:**
   - Всегда проверяйте подпись запроса
   - Ограничьте доступ по IP (если возможно)
   - Используйте HTTPS

4. **Логирование:**
   - Включите логирование всех деплоев
   - Регулярно проверяйте логи на ошибки

---

## 🚀 Быстрый старт (Рекомендуемый вариант)

### GitHub Actions + Webhook (Гибридный подход)

1. **Настройте GitHub Actions** (Вариант 1) для основного деплоя
2. **Добавьте Webhook** (Вариант 3) как резервный вариант
3. **Используйте bash скрипт** (Вариант 4) для ручных деплоев

### Минимальная настройка:

```bash
# 1. Создайте SSH ключ
ssh-keygen -t rsa -b 4096 -C "deploy"

# 2. Скопируйте на сервер
ssh-copy-id -i ~/.ssh/id_rsa.pub user@trendagent.siteaccess.ru

# 3. Добавьте приватный ключ в GitHub Secrets

# 4. Готово! Теперь при каждом push в main будет автоматический деплой
```

---

## 📊 Сравнение вариантов

| Критерий | GitHub Actions | Post-Receive | Webhook | Bash Script | PM2 |
|----------|----------------|--------------|---------|-------------|-----|
| Сложность настройки | ⭐⭐⭐ | ⭐⭐ | ⭐⭐ | ⭐ | ⭐⭐⭐ |
| Автоматизация | ✅✅✅ | ✅✅✅ | ✅✅✅ | ✅ | ✅✅ |
| Логирование | ✅✅✅ | ✅ | ✅✅ | ✅ | ✅✅ |
| Безопасность | ✅✅✅ | ✅✅ | ✅✅ | ✅✅ | ✅✅ |
| Откат изменений | ✅✅ | ❌ | ❌ | ❌ | ✅ |
| Уведомления | ✅✅✅ | ❌ | ✅ | ❌ | ✅ |

---

## 🛠️ Troubleshooting

### Проблема: Деплой не запускается

1. Проверьте логи:
   - GitHub Actions: `Actions` tab в репозитории
   - Webhook: `~/deploy.log` на сервере
   - Post-receive: `~/deploy.log` на сервере

2. Проверьте права доступа:
```bash
ls -la ~/trendagent.siteaccess.ru/public_html/
chmod -R 755 ~/trendagent.siteaccess.ru/public_html/
```

3. Проверьте SSH подключение:
```bash
ssh user@trendagent.siteaccess.ru
```

### Проблема: Ошибки при сборке

1. Проверьте версию Node.js:
```bash
node -v  # Должна быть 18+
```

2. Очистите кеш:
```bash
rm -rf node_modules package-lock.json
npm install
```

---

## 📝 Дополнительные улучшения

### 1. Добавьте уведомления (Telegram/Discord/Slack)

Создайте `scripts/notify.sh`:
```bash
#!/bin/bash
curl -X POST "https://api.telegram.org/bot<TOKEN>/sendMessage" \
  -d "chat_id=<CHAT_ID>&text=Деплой завершен успешно!"
```

### 2. Добавьте проверку перед деплоем

```bash
# Проверка тестов перед деплоем
npm test || exit 1
```

### 3. Создайте staging окружение

Добавьте отдельный workflow для staging окружения, который деплоится при push в `develop` ветку.

---

## ✅ Чеклист настройки

- [ ] Выбрать вариант автоматизации
- [ ] Настроить SSH ключи
- [ ] Настроить секреты (GitHub Secrets / Webhook secret)
- [ ] Протестировать деплой
- [ ] Настроить логирование
- [ ] Настроить уведомления (опционально)
- [ ] Документировать процесс для команды

---

**Рекомендация:** Начните с **GitHub Actions** - это самый надежный и удобный вариант для автоматического деплоя.

