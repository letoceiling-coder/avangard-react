# 🚨 КРИТИЧЕСКАЯ БЕЗОПАСНОСТЬ

## ⚠️ ВАЖНО: НЕ ПОКАЗЫВАЙТЕ ТОКЕНЫ!

### ❌ НИКОГДА НЕ ДЕЛАЙТЕ:

1. **НЕ отправляйте токены в чаты/сообщения**
2. **НЕ показывайте токены на скриншотах**
3. **НЕ коммитьте токены в git**
4. **НЕ храните токены в открытом виде**

---

## ✅ ПРАВИЛЬНОЕ ИСПОЛЬЗОВАНИЕ ТОКЕНОВ

### Для GitHub Actions:

1. **Создайте токен:**
   - GitHub → Settings → Developer settings → Personal access tokens
   - Generate new token (classic)
   - Scopes: только `repo`
   - Expiration: 90 дней

2. **Добавьте в Secrets:**
   - Репозиторий → Settings → Secrets and variables → Actions
   - New repository secret
   - Name: `GITHUB_TOKEN` (или другое имя)
   - Value: ваш токен
   - Add secret

3. **Используйте в workflow:**
```yaml
env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

### Для SSH деплоя (рекомендуется):

**Вместо токенов используйте SSH ключи:**

1. **Создайте SSH ключ:**
```bash
ssh-keygen -t rsa -b 4096 -C "github-actions-deploy"
# Сохраните как: ~/.ssh/github_deploy_key
# БЕЗ пароля (для автоматизации)
```

2. **Добавьте публичный ключ в GitHub:**
   - Settings → SSH and GPG keys
   - New SSH key
   - Вставьте содержимое `~/.ssh/github_deploy_key.pub`

3. **Добавьте приватный ключ в Secrets:**
   - Репозиторий → Settings → Secrets → Actions
   - Name: `SSH_PRIVATE_KEY`
   - Value: содержимое `~/.ssh/github_deploy_key`

4. **Используйте в workflow:**
```yaml
- name: Setup SSH
  uses: webfactory/ssh-agent@v0.7.0
  with:
    ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
```

---

## 🔄 ЕСЛИ ТОКЕН СКОМПРОМЕТИРОВАН

### Немедленные действия:

1. ✅ **Удалите токен в GitHub**
2. ✅ **Проверьте Security log**
3. ✅ **Создайте новый токен**
4. ✅ **Обновите все места использования**

---

## 📋 ЧЕКЛИСТ БЕЗОПАСНОСТИ

- [ ] Все токены имеют срок действия
- [ ] Токены имеют минимальные права
- [ ] Нет токенов в git истории
- [ ] Секреты хранятся в GitHub Secrets
- [ ] SSH ключи защищены (chmod 600)
- [ ] `.env` файлы в `.gitignore`

---

## 🛡️ ЛУЧШАЯ ПРАКТИКА

**Используйте SSH ключи вместо токенов для деплоя!**

SSH ключи:
- ✅ Более безопасны
- ✅ Можно отозвать в любой момент
- ✅ Не имеют срока действия (но можно ограничить)
- ✅ Лучше для автоматизации

---

**ПОМНИТЕ: Токен = пароль. Не показывайте его никому!**

